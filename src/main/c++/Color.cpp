#include "Color.h"
#include <string>
using namespace std;

Color::Color(string strColor) {
//    int color = getEnumFromString(strColor);
}

Color::Color() {
//    int color = getEnumFromString(strColor);
}

Color::Color(int color) {
    this->color_ = color;
}
